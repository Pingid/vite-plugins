/**
 * Augmented by the file the plugin generates (`src/virtual.d.ts` by default).
 * No index signature: it would collapse every key to `any` and defeat the
 * generated narrowing.
 */
export interface VirtualModule {}
